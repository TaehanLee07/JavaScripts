<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <script>
        let result = "10+20"
        document.write(result + "<br>")
        let result1 = eval("10+20")
        document.write(result1 + "<br>")

        let num = 100
        document.write("num" + 10 + "<br>")
        document.write(eval("num") + 10 + "<br>")

        let result2 = isNaN("13")
        document.write(result2)


        // 자바스크립트의 내장함수 내장함수란 사용자의 편의를 위해 제공되는 함수

        eval() // 문자열을 자바스크립트 문장으로 변환 수식은 계산식으로 문자는 변수로
        parseInt()  // 문자열을 숫자를 정수로
        parseFloat() // 모든형태의 숫자를 실수로
        isNaN() // not a number 의 약자 숫자가 포함되어있으면 false 포홤되몆ㄴ true
        isFinite() // 주어진수가 유리수인지 확인
        Number() // 문자를 숫자형으로
        String() // 숫자를 문자형으로
        escape() // 문자를 16진수 아스키 코드로
        unescape() // 16진수 아스키 코드를 문자로 



    </script>
</head>

<body>

</body>

</html>
